package com.muralihub.monad

object Exercise4_4_5  {
//what is best

  // From TransformingEithers for result r1 and r2 returns Unit becuase it just prints

  // we can improve this by extending Throwable to trait LoginError so it throws if its left
  // we are not collecting what type of Error it is
  // we should report the error as quickly as we can







}
